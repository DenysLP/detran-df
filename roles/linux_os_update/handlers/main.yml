---
# handlers/main.yml — linux_os_update
# O reboot é tratado inline em tasks/main.yml (lógica auto/true/false).
- name: Reboot host
  ansible.builtin.reboot:
    msg: "{{ linux_os_update_reboot_msg }}"
    reboot_timeout: "{{ linux_os_update_reboot_timeout }}"
